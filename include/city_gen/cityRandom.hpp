#pragma once

namespace Random {

    // Returns an integer between a and b
    int GetIntBetweenInclusive(int a, int b);
    
    // Returns a float between a and b inclusive
    float GetFloatBetweenInclusive(float a, float b);

    // Return a float between 0 and 1 inclusive
    float GetPercentage(void);
}

